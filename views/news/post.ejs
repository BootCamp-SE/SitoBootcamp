<!DOCTYPE html>
<html lang="it" data-bs-theme="dark">
	<%- include('../partials/header.ejs') %>

	<body>
		<%- include('../partials/navbar.ejs') %>

		<div class="container mt-5 border border-secondary rounded" style="--bs-border-opacity: .5">
			<img src="<%= article.image %>" width="100px" height="100px">
			<h4><%= article.title %></h4>
			<h6 class="text-secondary"><%= article.subtitle %></h6>
			<% article.tags.forEach(tag => { %>
				<span class="badge bg-secondary"><%= tag %></span>
			<% }) %>
			<p class="text-secondary"><%= article.author %>, <%= article.date.toLocaleDateString() %></p>
			<hr>
			<%- article.body %>
		</div>
	</body>

	<%- include('../partials/footer.ejs') %>
</html>
